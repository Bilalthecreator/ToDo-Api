# ToDo-Api
A full-featured RESTful API built with FastAPI and SQLAlchemy that enables users to manage tasks with group organization, JWT authentication, and a responsive web interface. Supports complete CRUD operations, task filtering by group/status, and secure user authentication with password hashing.
